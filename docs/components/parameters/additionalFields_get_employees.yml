name: additionalFields
in: query
description: 指定されたプロパティをレスポンスに追加
explode: false
schema:
  type: array
  items:
    type: string
    enum:
      - lastNamePhonetics
      - firstNamePhonetics
      - hiredDate
      - birthDate
      - resignationDate
      - emailAddresses
      - allDayRegardingWorkInMinute
  example:
    - emailAddresses
