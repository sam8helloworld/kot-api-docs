type: array
items:
  type: object
  required:
    - date
    - dailyWorkings
  properties:
    date:
      type: string
      format: date
    dailyWorkings:
      type: array
      items:
        $ref: "../schemas/daily_working_timerecord_response.yml"
