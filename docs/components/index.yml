parameters:
  token:
    $ref: "./parameters/token.yml"
  additionalFields:
    $ref: "./parameters/additionalFields.yml"
  date:
    $ref: "./parameters/date.yml"
  division:
    $ref: "./parameters/division.yml"
  includeResigner:
    $ref: "./parameters/includeResigner.yml"

securitySchemes:
  Bearer:
    type: http
    scheme: bearer
    description: Credentials or access token for API
